# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

---

## フェーズ1: 実装

- [x] `createOrUpdateCalendarEvent()` に6時間超過チェックを追加
  - [x] 「開始と終了日時の逆転チェック」の直後に挿入
  - [x] 既存パターンと同一構造（イベント削除→フラグ設定→return）

- [x] `sendAggregatedEmails()` に6時間超過の通知メール分岐を追加
  - [x] `endTime < startTime` 分岐の直後、`else`（正常登録）の直前に挿入
  - [x] メール件名・本文は既存エラーケースと統一

## フェーズ2: コードレビュー

- [x] 変更後のコード全体を確認
  - [x] バリデーションの順序が正しいか（過去日時→逆転→6時間超過→登録）
  - [x] 既存ロジックに影響がないか
  - [x] コーディング規約（インデント、コメント形式）に準拠しているか

## フェーズ3: ドキュメント更新

- [x] `docs/functional-design.md` を更新（バリデーションルールに6時間超過を追加）
- [x] コミット

---

## 実装後の振り返り

### 実装完了日
2026-03-12

### 計画と実績の差分

**計画と異なった点**:
- なし。設計書通りに実装完了。

**学んだこと**:
- 既存のバリデーションパターンが統一されていたため、同一構造を複製するだけで済んだ。設計フェーズで挿入位置とコードを明確に定義しておくことで、実装がスムーズに進んだ。
